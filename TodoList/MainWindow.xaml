<Window x:Class="TodoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:taskbar="http://www.hardcodet.net/taskbar"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TodoList"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" SelectionChanged="ListView_SelectionChanged" Grid.ColumnSpan="2">
            <DataGrid Name="TasksDataGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="10" Height="172" Width="524" SelectionChanged="TasksDataGrid_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Monday" Binding="{Binding Monday}" Width="Auto"/>
                    <DataGridTextColumn Header="Tuesday" Binding="{Binding Tuesday}" Width="Auto"/>
                    <DataGridTextColumn Header="Wednesday" Binding="{Binding Wednesday}" Width="Auto"/>
                    <DataGridTextColumn Header="Thursday" Binding="{Binding Thursday}" Width="Auto"/>
                    <DataGridTextColumn Header="Friday" Binding="{Binding Friday}" Width="Auto"/>
                    <DataGridTextColumn Header="Saturday" Binding="{Binding Saturday}" Width="Auto"/>
                    <DataGridTextColumn Header="Sunday" Binding="{Binding Sunday}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>
            <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center" Margin="10">
                <Button Name="PrevWeekButton" Content="Previous Week" Width="100" Margin="5" Click="PrevWeekButton_Click"/>
                <Label Name="CurrentWeekLabel" Content="15/07/2024 - 20/07/2024" Margin="10" VerticalAlignment="Center"/>
                <Button Name="NextWeekButton" Content="Next Week" Width="100" Margin="5" Click="NextWeekButton_Click"/>
            </StackPanel>
        </ListView>
        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" Margin="0,394,0,0" Grid.RowSpan="2">
            <TextBox x:Name="NewTodoTextBox" Width="150" Margin="10" TextChanged="NewTodoTextBox_TextChanged"></TextBox>
            <Button Content="Add Todo" Click="AddTodoButton_clicked" Margin="10"></Button>
            <Button Name="UpdateButton" Content="Update" Width="75" Margin="5"/>
            <Button Name="DeleteButton" Content="Delete" Width="75" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>
